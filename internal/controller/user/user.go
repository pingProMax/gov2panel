// =================================================================================
// This is auto-generated by GoFrame CLI tool only once. Fill this file as you wish.
// =================================================================================

package user

import (
	"context"
	"fmt"

	"github.com/gogf/gf/v2/frame/g"
)

func setTplUser(ctx context.Context, dir string, data g.Map) {

	d := g.Map{
		"mainTpl":   fmt.Sprintf("user/%s/index.html", dir),
		"headTpl":   fmt.Sprintf("user/%s/head.html", dir),
		"scriptTpl": fmt.Sprintf("user/%s/script.html", dir),
		"path":      g.RequestFromCtx(ctx).Router.Uri,
	}
	for key, value := range data {
		d[key] = value
	}

	g.RequestFromCtx(ctx).Response.WriteTpl("user/user.html", d)
}
